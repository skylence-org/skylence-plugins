{
  "name": "skyline-mod-claude",
  "version": "0.2.0",
  "description": "EARLY ACCESS: skyline as a Claude Code mod (function hooks). One tool.call hook on Read answers a Read of a text file inside a code tree from the skyline daemon itself, in the record core produces, so the model's native Read succeeds at once instead of being denied and redirected by the classic enforce hook. Files outside any code tree, under ~/.claude, pdfs, images, notebooks, pages requests, daemon errors and a daemon that is down all fall through to core's Read untouched. Every result's shape is still recorded (keys and types, never contents). Written against Claude Code 2.1.278 with CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1; the mod API may change between releases.",
  "author": {
    "name": "Skylence",
    "url": "https://github.com/skylence-be"
  },
  "homepage": "https://github.com/skylence-be/skylence-plugins",
  "repository": "https://github.com/skylence-be/skylence-plugins",
  "license": "MIT",
  "keywords": [
    "skylence",
    "skyline",
    "mod",
    "function-hooks",
    "claude-code"
  ],
  "userConfig": {
    "daemonUrl": {
      "type": "string",
      "title": "Skyline daemon URL",
      "description": "The skyline daemon's MCP endpoint. Empty uses http://127.0.0.1:7333/mcp.",
      "required": false,
      "default": ""
    },
    "answerFromSkyline": {
      "type": "boolean",
      "title": "Answer Read from skyline",
      "description": "On (default): eligible Reads are answered from the skyline daemon. Off: the mod observes only, as stage 1 did.",
      "required": false,
      "default": true
    },
    "anchorContext": {
      "type": "boolean",
      "title": "Anchor context line",
      "description": "On (default): skyline's ¶path#TAG anchor rides along as one context line after a Read answered from skyline, so the model can paste it into a skyline edit. Off: the Read is fully transparent.",
      "required": false,
      "default": true
    },
    "shapeFile": {
      "type": "string",
      "title": "Shape file",
      "description": "Optional path (absolute, or relative to the working directory) where the deduplicated Read result shapes are written as JSON after every Read. Empty (the default) writes no file; shapes still go to the debug log, and each new shape is announced once in the transcript.",
      "required": false,
      "default": ""
    }
  }
}
